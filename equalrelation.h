#ifndef EQUALREALTION_H
#define EQUALREALTION_H

#include "variable.h"
#include <vector>;
using std::vector;

class EqualRelation{
public:
  Add(Variable &variable){
    vector<variable *>::iterator it；
    it = 
    if()　
    this->_variables.push_back(variable);
  }
private:
  vector<variable *> _variables;
};
#endif
